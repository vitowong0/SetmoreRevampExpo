import React from 'react';
import {View, Text, StyleSheet, TouchableOpacity} from 'react-native';

function ServicesAvailableItem({id, name, duration, cost}) {
  <View>
    <Text></Text>
  </View>;
}
